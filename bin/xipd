#!/usr/bin/env node

var xip = require('..');
var fs = require('fs');
var path = require('path');

var argv = process.argv;
if (argv.length < 3) {
  console.error("usage: xipd <config>");
  process.exit(1);
}

var config = require( path.resolve( argv[2] ) );

xip.run( config );
